{
  "installCommand": "pnpm install --no-frozen-lockfile && mkdir -p .hugo-bin && curl -fsSL https://github.com/gohugoio/hugo/releases/download/v0.162.0/hugo_extended_0.162.0_Linux-64bit.tar.gz | tar -xz -C .hugo-bin hugo",
  "buildCommand": "pnpm run build",
  "outputDirectory": "public"
}
